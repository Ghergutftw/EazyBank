{{- if .Values.cards.enabled }}
{{/*
Cards Microservice
Manages customer card operations
*/}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cards
  labels:
    {{- include "eazybank.microservice.labels" (dict "name" "cards" "context" .) | nindent 4 }}
spec:
  replicas: {{ .Values.cards.replicaCount }}
  selector:
    matchLabels:
      {{- include "eazybank.microservice.selectorLabels" (dict "name" "cards" "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "eazybank.microservice.selectorLabels" (dict "name" "cards" "context" .) | nindent 8 }}
    spec:
      containers:
      - name: cards
        image: {{ include "eazybank.image" (dict "repository" .Values.cards.image.repository "tag" .Values.cards.image.tag "context" .) }}
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.cards.containerPort }}
        env:
        {{- include "eazybank.springboot.env" . | nindent 8 }}
        {{- include "eazybank.resources" .Values.cards.resources | nindent 8 }}
        {{- include "eazybank.springboot.livenessProbe" (dict "path" .Values.cards.probes.liveness.path "port" .Values.cards.containerPort "initialDelay" .Values.cards.probes.liveness.initialDelaySeconds "period" .Values.cards.probes.liveness.periodSeconds "timeout" .Values.cards.probes.liveness.timeoutSeconds "failureThreshold" .Values.cards.probes.liveness.failureThreshold) | nindent 8 }}
        {{- include "eazybank.springboot.readinessProbe" (dict "path" .Values.cards.probes.readiness.path "port" .Values.cards.containerPort "initialDelay" .Values.cards.probes.readiness.initialDelaySeconds "period" .Values.cards.probes.readiness.periodSeconds "timeout" .Values.cards.probes.readiness.timeoutSeconds "failureThreshold" .Values.cards.probes.readiness.failureThreshold) | nindent 8 }}
---
apiVersion: v1
kind: Service
metadata:
  name: cards
  labels:
    {{- include "eazybank.microservice.labels" (dict "name" "cards" "context" .) | nindent 4 }}
spec:
  selector:
    {{- include "eazybank.microservice.selectorLabels" (dict "name" "cards" "context" .) | nindent 4 }}
  type: {{ .Values.cards.service.type }}
  ports:
    - protocol: TCP
      port: {{ .Values.cards.service.port }}
      targetPort: {{ .Values.cards.containerPort }}
{{- end }}

